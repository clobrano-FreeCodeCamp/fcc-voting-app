{{!< layout/main}}

{{#if username }}
{{else}}
<div class="jumbotron">
  <p>Voting APP for Free Code Camp</p>
  <p><a class="btn btn-primary btn-lg" href="https://www.freecodecamp.com/challenges/build-a-voting-app" role="button">Learn more »</a></p>
</div>
{{/if}}

<div class="container-fluid container-polls">
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>#</th>
      <th>Title</th>
      <th>Votes</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {{#each polls}}
      <tr>
        <th>{{inc @index}}</th>
        <td>{{title}}</td>
        <td>{{votes}}</td>
        <td><a href="/polls/show/{{_id}}"><span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span></a></td>
        {{#if (eq ../userid owner)}}<td><a href="/polls/edit/{{_id}}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a></td>{{/if}}
        {{#if (eq ../userid owner)}}<td><a href="/polls/erase/{{_id}}"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a></td>{{/if}}
      </tr>
    {{/each}}
  </tbody>
</table>


{{#if username}}
  <a class="btn btn-md btn-default" role="button" href="/polls/new">+ New Poll</a>
{{/if}}
</div>
